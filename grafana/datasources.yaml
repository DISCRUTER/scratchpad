apiVersion: 1

datasources:
  - name: Main
    uid: main-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      # Matches the Prometheus scrape_interval so panels pick sane steps.
      timeInterval: 5s
